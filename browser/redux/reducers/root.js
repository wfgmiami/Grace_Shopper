const initialState = {};

export default (state = initialState, action) => {
  let newState;
  switch (action.type) {
    case 'TODO':
      break;
    default:
      break;
  }

  return newState;
};
